{
  "name": "ar-directory",
  "private": true,
  "version": "0.7.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "data:generate": "node scripts/generate-ar-csv.mjs",
    "data:enrich": "node scripts/apply-enrichment.mjs",
    "og:generate": "node scripts/generate-og-images.mjs",
    "images:enrich": "node scripts/enrich-manufacturer-images.mjs",
    "test": "vitest run src/__tests__",
    "test:watch": "vitest src/__tests__",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:lighthouse": "npm run build && node scripts/lighthouse-ci.mjs",
    "verify": "npm test && npm run test:e2e && npm run build"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@tailwindcss/vite": "^4.3.2",
    "tailwindcss": "^4.3.2",
    "vite": "^8.1.4",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "papaparse": "^5.5.4"
  },
  "optionalDependencies": {
    "sharp": "^0.35.3"
  }
}
